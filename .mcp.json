{
  "mcpServers": {
    "fablecut": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server.js"],
      "env": {
        "FABLECUT_MCP_DATA_DIR": "${CLAUDE_PLUGIN_DATA}/mcp"
      }
    }
  }
}
